var input = $('#<%= "#{@bookmark.bookmarkable_type_with_id}-bookmark" %>');

if(input.length > 0) {
  input.replaceWith('<%= link_to bookmarks_path(bookmarkable_id: @bookmark.bookmarkable_id, bookmarkable_type: @bookmark.bookmarkable_type), remote: true, method: :post, id: "#{@bookmark.bookmarkable_type_with_id}-bookmark" do %> <i class="fas fa-save"></i> <%= t('articles.dropdown.save') %> <% end %>');
}