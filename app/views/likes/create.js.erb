var icon = $('#<%= @like.likeable_type_with_id %>-likes');
if(icon.length > 0) {
  var count = $('#<%= @like.likeable_type_with_id %>-likes-count');
  <% if @liked %>
    $('#<%= @like.likeable_type_with_id %>-likes').addClass("liked");
    count.text(Number(count.text()) + 1)
  <% else %>
    $('#<%= @like.likeable_type_with_id %>-likes').removeClass("liked");
    count.text(Number(count.text()) - 1)
  <% end %>
}
