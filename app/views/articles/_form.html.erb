<div class="article-form" id="article-form">
  <h3 class="text-center">Добавить новость</h3>
  <%= form_with model: @article, local: true  do |f| %>
    <div class="form-group">
      <%= f.text_area :body, class: "form-control", rows: 3, placeholder: "Новость" %>
    </div>
    <div class="form-group">
      <%= f.file_field :picture %>
    </div>
    <div>
      <%= f.submit "Опубликовать", class: "btn btn-primary float-right" %>
    </div>
  <% end %>
</div>